build_settings:
  verbose: true
  ignore:
    - "vendor"

test:
  php_loc:
    directory: "src"
  php_unit:
    config:
        - "phpunit.xml"
    path: "tests/"

setup:
  composer:
    action: "install"
    prefer_dist: false